<!--- TEMPLATENAME: failed.isml --->
<isdecorate template="checkout/pt_checkout">
    <h2>${Resource.msg('cko.transaction.failed', 'cko', null)}</h2>
    <p>${Resource.msg('cko.transaction.failedMessage1', 'cko', null)}</p>	
    <p>${Resource.msg('cko.transaction.failedMessage2', 'cko', null)}</p>	

    <ul>
        <li>
            <p>
                <a href="${URLUtils.https('Cart-Show')}">${Resource.msg('cko.new.payment', 'cko', null)}</a>
            </p>
        </li>
        <li>
            <p>
                <a href="${URLUtils.https('Account-Show')}">${Resource.msg('cko.view.account', 'cko', null)}</a>
            </p>
        </li>
    </ul>
</isdecorate>